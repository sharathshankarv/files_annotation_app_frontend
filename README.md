# Frontend (Next.js)

## Preferred way (from repo root)

```bash
pnpm install
pnpm dev:frontend
```

Or run both apps together:

```bash
pnpm dev
```

## Local-only run (from this folder)

```bash
pnpm install
pnpm dev
```

Default app URL: [http://localhost:3000](http://localhost:3000)
